local player = game.Players.LocalPlayer
if player then
    local customMessage = "Script Outdated, Check DenDenZ On YouTube for updated Version"
    player:Kick(customMessage)
else
    warn("LocalPlayer not found.")
end
